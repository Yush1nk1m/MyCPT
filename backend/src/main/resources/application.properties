spring.application.name=backend

# Claude API
llm.anthropic.api-key=${ANTHROPIC_API_KEY}
llm.anthropic.model=claude-sonnet-4-6
llm.anthropic.max-tokens=4000

# disc_cache TTL (온디맨드 만료 기준)
cache.disc.ttl-days=365